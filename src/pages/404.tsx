import React from 'react';
import { Button, Input } from 'antd'

const NotFound: React.FC = () => {
  return (
    <div>
      <Button>test</Button>
      <Input />
      <p>默认缺省页</p>
    </div>
  );
};

export default NotFound;
